/* exported head */
/* 🚨 Do not use slice(), splice(), pop(), or shift() in your implementation of head.
/* Define a function named head with one parameter, array.
array - Any JavaScript Array
This function should Return The first element of array. */
function head(array) {
  return array[0];
}
