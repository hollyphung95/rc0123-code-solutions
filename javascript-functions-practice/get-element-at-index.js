/* exported getElementAtIndex */
/* Define a function, named getElementAtIndex with two parameters’, any javascript array and a number index.   ,   .
Declare Create a variable named result and assign it to null;
use brackets to access the array at given index. assign that value to result.
return result.  The return value should be The element in array at the specified index. */
function getElementAtIndex(array, index) {
  var result = null;
  result = array[index];
  return result;
}
