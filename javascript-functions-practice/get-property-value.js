/* exported getPropertyValue */
/* Define a function, named ‘getPropertyValue’, that accepts two arguments,an object and a key property.
Declare Create a variable named ‘result’ and assign null to it.
use brackets to access the array at the given key.
The return value should be The value stored in the object at the specified property key.
return result. */
function getPropertyValue(object, key) {
  var result = null;
  result = object[key];
  return result;
}
