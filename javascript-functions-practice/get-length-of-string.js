/* exported getLengthOfString */
/* Define a function, named getLengthOfString(string)’, that accepts one argument, array  ,   .
Declare Create a new variable length.
Asscess get the length property of the string given.
Assign that value to 'length' variable.
return should be The length of the input string. */
function getLengthOfString(string) {
  var length = 0;
  length = string.length;
  return length;
}
