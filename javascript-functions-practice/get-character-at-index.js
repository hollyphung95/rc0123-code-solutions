/* exported getCharacterAtIndex */
/* getCharacterAtIndex(string, index)
string - a string
index - a numeric index greater than or equal to 0
The character in string at the specified index. */
function getCharacterAtIndex(string, index) {
  var charFound = '';
  charFound = string[index];
  return charFound;
}
